# Native Windows / no Docker — copy to backend\.env:
#   copy env.native.example .env
DATABASE_URL=sqlite:///./product_matching.db
MAX_UPLOAD_SIZE_MB=200
EMBEDDING_PROVIDER=tfidf
QDRANT_LOCAL_PATH=./qdrant_storage
ENABLE_LOW_CONFIDENCE_AUTO_REJECT=false
RERANKER_PROVIDER=rrf
