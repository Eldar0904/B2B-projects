rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // TEMPORARY (building phase): open access, no Firebase Auth required.
    // Re-lock before production — restore `if request.auth != null` on each match.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
